<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/royal26"
    android:fitsSystemWindows="true"> <!-- حماية أولية من النظام -->

    <!-- ================= المحتوى الرئيسي ================= -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- تظليل خلفي للملكية -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#9900081A" />

        <!-- الشريط العلوي (Custom Toolbar) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/top_bar"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="#4DFFFFFF"
            app:layout_constraintTop_toTopOf="parent">

            <ImageButton
                android:id="@+id/btn_menu"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:background="?selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_sort_by_size"
                app:tint="@color/royal_accent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

            <TextView
                android:id="@+id/tv_header_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:textStyle="bold"
                app:layout_constraintStart_toEndOf="@+id/btn_menu"
                app:layout_constraintEnd_toStartOf="@+id/btn_language"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

            <ImageButton
                android:id="@+id/btn_language"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="8dp"
                android:background="?selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_manage" 
                app:tint="@color/royal_accent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>
        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- الأزرار المركزية بأسلوب البطاقات الزجاجية -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="20dp"
            app:layout_constraintTop_toBottomOf="@+id/top_bar"
            app:layout_constraintBottom_toTopOf="@+id/banner_container">

            <!-- زر تحميل أورج 2027 الملكي -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_download_2027"
                android:layout_width="match_parent"
                android:layout_height="75dp"
                android:text="@string/download_2027"
                android:textColor="@color/royal_accent"
                android:textSize="18sp"
                app:backgroundTint="#33FFFFFF"
                app:strokeColor="@color/royal_accent"
                app:strokeWidth="1.5dp"
                app:cornerRadius="20dp"
                android:layout_marginBottom="20dp"/>

            <!-- زر تحميل زيكو VIP -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_download_vip"
                android:layout_width="match_parent"
                android:layout_height="75dp"
                android:text="@string/download_vip"
                android:textColor="@color/white"
                android:textSize="18sp"
                app:backgroundTint="#33FFFFFF"
                app:strokeColor="@color/white"
                app:strokeWidth="1.5dp"
                app:cornerRadius="20dp"
                android:layout_marginBottom="20dp"/>

            <!-- زر معاينة الأورج -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_preview"
                android:layout_width="match_parent"
                android:layout_height="75dp"
                android:text="@string/preview_org"
                android:textColor="@color/black"
                android:textSize="18sp"
                app:backgroundTint="@color/royal_accent"
                app:cornerRadius="20dp"/>

        </LinearLayout>

        <!-- حاوية البانر الإعلاني المتكيفة -->
        <FrameLayout
            android:id="@+id/banner_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="50dp"
            android:background="#1A000000"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ================= القائمة الجانبية (Drawer) ================= -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#F20A0E29"
        app:itemTextColor="@color/white"
        app:itemIconTint="@color/royal_accent"
        app:menu="@menu/drawer_menu">
        
        <!-- حقوق النشر في أسفل القائمة -->
        <TextView
            android:id="@+id/tv_copyright"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:padding="16dp"
            android:text="@string/copyright"
            android:textColor="@color/royal_accent"
            android:textAlignment="center"
            android:textSize="12sp"
            android:textStyle="bold" />
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>

